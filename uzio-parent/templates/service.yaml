apiVersion: v1
kind: Service
metadata:
  name: {{ include "uzio-parent.fullname" . }}
  labels:
    {{- include "uzio-parent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "uzio-parent.selectorLabels" . | nindent 4 }}
