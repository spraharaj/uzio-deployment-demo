apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uzio-parent.fullname" . }}-test-connection"
  labels:
    {{- include "uzio-parent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "uzio-parent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
